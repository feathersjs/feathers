import populateHeader from './populate-header';
import populateAccessToken from './populate-access-token';
import populateEntity from './populate-entity';

let hooks = {
  populateHeader,
  populateAccessToken,
  populateEntity
};

export default hooks;
