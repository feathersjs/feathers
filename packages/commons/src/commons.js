import getArguments from './arguments';
import {
  _,
  stripSlashes,
  matcher,
  sorter,
  select,
  makeUrl,
  // lodash functions
  each,
  some,
  every,
  keys,
  values,
  isMatch,
  isEmpty,
  isObject,
  extend,
  omit,
  pick,
  merge
} from './utils';
import hooks from './hooks';

export default {
  _,
  getArguments,
  stripSlashes,
  hooks,
  matcher,
  sorter,
  select,
  makeUrl,
  // lodash functions
  each,
  some,
  every,
  keys,
  values,
  isMatch,
  isEmpty,
  isObject,
  extend,
  omit,
  pick,
  merge
};
